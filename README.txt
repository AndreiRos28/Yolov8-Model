## ⚙️ Installation
1. Clone this repo:  
   ```bash
   git clone https://github.com/yourusername/human-tracker.git
   cd human-tracker
Create a virtual environment (recommended):

bash
Copy code
python -m venv .venv
.venv\Scripts\activate      # Windows  
source .venv/bin/activate   # Linux/Mac  
Install dependencies:

bash
Copy code
pip install -r requirements.txt

🎯 Usage

Run the tracker like this:

cd scripts
py track.py


Controls:

🖱️ Left-click & drag → Draw a geofence rectangle.

⌨️ Press q → Quit the program.